// Third-party credentials that ship as placeholders — see SETUP.md.
//
// Fishial.AI (fish species identification from a photo): sign up for a
// developer account at https://portal.fishial.ai to get real values here.
// Like the Google Maps API keys in the native projects, this repo cannot
// ship a working key/secret — FishIdService will fail until these are set.
const fishialClientId = 'YOUR_FISHIAL_CLIENT_ID';
const fishialClientSecret = 'YOUR_FISHIAL_CLIENT_SECRET';
